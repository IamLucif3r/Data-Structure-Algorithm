Algorithm for solving question:
 for(int i=2;i<=n;i++)
    {
        flag = 0 ;
        sout(2");
        if(i%2 != 0 || i == 2)
        {
            flag=1;
            for(int j=2;j<=sqrt(i);j++)
            {
                if(i%j==0)
                {
                    flag=0;
                    break;
                }
            }
        }
        if(flag)
            S.O.P(i);
    }